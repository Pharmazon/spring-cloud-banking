server.port: 8600
spring:
  application.name: ConfigServer
  cloud:
    config:
      server:
        bootstrap: true
        git:
          force-pull: true
          uri: https://github.com/Pharmazon/spring-cloud-banking-configs.git
